# 更新日志

## v0.1.3

本次以**两个静默 bug 的修复**为主，另加了指令审计与消息占位。

### 修复

- **QQ 侧 AI 回复从不进入游戏公屏**（严重）。原设计里那条回复要先进 QQ 群、
  再靠「机器人自己的消息回环成事件」转发进游戏，而实测 **NapCat 根本不上报
  机器人自己发的消息**——那条路从来就不可能工作，`sync_bot_msgs` 打开也只是
  放行一个永远收不到的事件。改用 AstrBot 的 `on_decorating_result` 钩子直接
  取即将发出的回复，不经过 NapCat。

- **绑定群名单为空时 QQ→MC 整个方向失效**。把名单从 `target_groups` 合并进
  `group_names` 时引入：存量部署只有旧键，新键解析为空 → 一切 QQ→MC 被挡掉，
  且**完全静默**（不报错、插件照常在群里回话）。现在 `group_names` 为空时
  回退读旧键并记 warning。

### 新增

- **指令审计日志**（`log_command_audit`，默认开）。此前失败路径各有日志，
  但**成功执行的指令一条都不记**——出事时（谁用 AI 清了一片地）翻不出来是谁
  干的。现在每次经 AI 执行指令都记一条，含来源侧、发起者、目标服务器、
  好感消耗与指令原文。

- **启动时检测已废弃的配置项**。AstrBot 只补缺失的键、从不覆盖已有值，所以
  插件改默认值对存量部署毫无动静。现在启动时若发现已删除的配置项仍有值，
  会记一条 WARNING 提示清理。（**与默认值不同**的项只记 INFO 计数——
  那多半是你的自定义，全报出来是噪声。）

- **图片/表情等非文本消息转占位符**。此前群友发张图，游戏里**什么都没发生**，
  发的人以为插件坏了。现在转成 `[图片]`/`[表情]`/`[语音]` 等。

### 变更

- **提示词与消息模板默认值按生产实践更新**：指令价目表扩写（含变量判定与
  分级计价）、成就提示词补全输出要求与难度分档、游戏侧上下文细化物品规则、
  三个消息模板的配色与括号格式调整。

  ⚠️ **已保存过配置的部署不会自动获得这些新默认值**——AstrBot 返回的是当年
  落盘的那份。想用新默认值，请到 WebUI 把对应配置项**重置为默认**。

- 全角标点统一为半角（提示词里全角标点占更多 token）。

### 修复（文档）

- 插件 README 修掉三处渲染问题，其中一处是**代码块被四个反引号关闭**——
  会导致后半篇文档被吞进代码块。

## v0.1.2

### 变更

- ⚠️ **配置项合并：`target_groups` 已删除**，绑定群名单并入 `group_names`。
  这两项的**键完全重复**（一边填绑定群号、一边填群号与群名映射），
  改一个忘一个就静默失效。现在 `group_names` 一肩挑：
  **键**是绑定群号，**值**是群名（只填群号时显示为群号）。
  **升级后请把群号补进 `group_names`**（如 `123456:水群`，或只写 `123456`）。
  原 `target_groups` 里的值**不会自动迁移**——不补的话消息就不互通了。
  之所以不自动搬：静默迁移会让「明明删掉的群还在转发」这种状态出现。

- **指令价目表调整**（`mc_command_cost_param_desc` 的默认值）：
  高级物品（龙蛋 / 下界之星 / 鞘翅）**10 → 30** 起；
  原版无法获得的物品（超上限附魔、指定数值的生物等）**15 → 20** 起；
  严重超标 **50** 起；传送类改为按距离计费（每千米 +1）；
  免费档不再包含「玩家自己短距传送」。

  ⚠️ **想拿到新价目表，请到 WebUI 把该项「重置为默认」**——已保存过配置的部署
  返回的是当年落盘的那份默认值，改代码对它无效（这是 AstrBot 的行为，不是 bug）。

- **成就提示词**：AI 收到成就通知后的回应由「并发表看法」改为「随便说说」，语气更松。

- **`enable_death` 的说明补回键名**：改成同时点明它门控的是 `karma_death_penalty`，
  用户才知道要调哪一项，而不是只看到「同时门控死亡扣好感」。

### 修复

- **插件 README**：修掉三处会让人看不懂的渲染问题——一个代码块被四个反引号
  关闭（导致后半篇被吞进代码块）、一个句号被甩到单独一行、引用行吃掉后面的列表。

- **文档内部**：`claude.md` 的行尾表有两处记反（`CHANGELOG.md` 实际是 CRLF、
  `README.md` 实际是 LF），已按 `git ls-files --eol` 修正。

## v0.1.1

本次以**配置项体验**为主：好感范围可配、多个配置项改成富列表编辑器，
另外修了一批会让配置**静默失效**的坑。

### 新增

- **好感度范围可配**（`karma_min` / `karma_max`，默认仍是 -50 / 100）。
  此前范围硬编码，用户在 `karma_rules` 提示词里写更大的范围**不会生效**，
  且完全静默。现在：
  - `karma_rules` 默认值里的数字改成占位符 `{karma_min}` / `{karma_max}` /
    `{karma_initial}`，插件启动时按**实际配置**渲染——改配置项即可，不必再翻提示词；
  - 上下限倒置（`min >= max`）会回退默认并记 warning——不挡的话所有人的好感
    会被钉死在同一个值上；
  - 初始值会被夹进配置的范围，不会出现「初始值越界」。
- **7 个配置项改成列表编辑器**：`group_names` / `admin_qq` / `admin_mc` /
  `group_names` / `server_display_names` / `ws_ports` / `mc_wake_prefixes`。
  在 WebUI 里变成带「添加 / 批量导入 / 删除」的列表，不必再手写
  「123456,789012」这种逗号分隔串。

### 变更

- **成就提示词改口径**：「并主动对他道贺」改为「并发表看法」，
  并新增「但不要暗示好感度的变化」。
- **插件详情页的函数说明去掉 markdown**：AstrBot 用 docstring 原样渲染
  「事件监听器 / 钩子 / LLM 工具」几块，而那里不解析 markdown——
  原先满屏的 `**⚠️ ...**` 与反引号会原样显示成噪声。已精简改写。

### 修复（配置静默失效类）

- **`str()` 包装毁掉列表值**：`server_display_names` / `group_names` 的读取处
  原先包着 `str()`，配置项改 list 后正好被它毁掉——整份列表被转成
  `"['a','b']"` 这种字符串，解析出来是带引号的怪键。
- **列表里的单个条目也可能含分隔符**：从别处整段粘贴 `"a,b"` 到一条里时，
  不切开就会留下永远匹配不上的条目。
- **无法识别的类型**（如手改成整数）按空处理并记 warning，不再臆造条目。

### 升级须知

- **已部署的实例不需要做任何事**：新增的 `karma_min` / `karma_max` 会被
  AstrBot 自动补上默认值；那 7 个配置项**仍然读得懂你原来填的逗号分隔字符串**
  （解析层同时接受字符串与列表两种形态——AstrBot 不会覆盖用户已有的值，
  所以存量实例拿到的仍是旧字符串）。
- **但「好感范围自动跟随」需要你改一下**：若你此前把范围写死在 `karma_rules`
  提示词里，那段文字仍在（自定义文本不含占位符就原样使用）。想让它跟着
  `karma_max` 走，把数字换成 `{karma_max}` 即可。
- ⚠️ **`target_groups` 已删除，名单合并进 `group_names`**：这两项原本键完全重复
  （一边填绑定群号、一边填群号与群名映射），改一个忘一个就静默失效。
  现在 `group_names` 一肩挑：**键**是绑定群号，**值**是群名（只填群号则显示为群号）。
  **升级后请把你原来的群号补进 `group_names`**（如 `123456:水群`，或只写 `123456`）。
  留空的 `target_groups` 会被忽略，**不会**自动迁移——不补的话消息就不互通了。

---

## v0.1.0

本版把**游戏内对话改走 AstrBot 的原生管线**，并修掉几处实测缺陷。

### 重大变更

- **游戏内对话改用原生平台适配器**：新增一个由插件注册的平台（每台 MC 服务器
  对应一个「群」，umo 形如 `netherlink_mc:GroupMessage:<server_id>`），
  玩家在游戏里说话会变成一个**真实的 AstrBot 事件**，走完整管线。
  好处：人格 / 记忆 / 工具 / 工具状态提示 / 出站推送全部复用框架，
  不再由插件自己维护一套。
  - 旧的自建 agent 路径（自己造合成事件、自己造工具集、自己拼提示词）**已整体删除**。
  - 开关 `enable_game_platform` 默认开启；**首次启用需要重启一次 AstrBot**
    （框架在启动时加载平台）。适配器不可用时插件会记日志并如实告知玩家。
- **配置项合并**：「游戏内自定义提示词」（原 `extra_system_prompt`）并进了
  游戏侧的系统上下文模板（`template_netherlink_context_game`），两者现在是
  同一个配置项。原「游戏内对话的附加说明模板」与内部占位符 `{origin}` /
  `{roster}` 及其开关 `admin_context_admin_only` **已删除**——前者与 AstrBot
  自带的 Group name 重复，后两者对「是不是管理员」没有增量信息。
  两个场景的上下文模板（`..._qq` / `..._game`）**保留**，仍然可配。
- **删除了 `template_netherlink_request`**（游戏侧**用户消息**里的工具说明）
  及其配套渲染函数——走原生管线后用户消息由框架构造，插件不再自己拼。
  这也让「同名标签发两份、AI 采信残缺的那份」这条历史坑从根上消失。

### 修复

- **回复不再分成两段**：此前游戏侧走插件自建的 agent，步数上限只有 6，
  「查好感 → 报价 → 执行 → 组织回复」很容易用尽，框架会拔掉工具并强推一段
  收尾。改走原生管线后由框架的 `agent_runner` 统一调度，这个问题不再存在。
- **指令不再落到上一个说话人身上**：会话按服务器建，玩家消息原先不带署名，
  AI 会把请求挂到历史里最近出现的人头上。现在身份经
  `event.set_extra("netherlink_ctx")` 传递（不再依赖闭包），
  说话人由 AstrBot 自己附带的群名与玩家 ID 交代。
- **AI 回复同步到所有绑定群**：此前 `sync_qq` 作为参数穿过三层，漏传两次
  （流式分支、`send_by_session` 分支）且默认关闭，失败完全静默。
  现在只在**一个决策点**判断：AI 的最终回复推群，工具调用/思考那类过程消息
  只进游戏公屏。
- **插件改了代码但游戏里跑旧逻辑**：AstrBot 热重载只替换平台**类**、不停掉
  正在运行的**实例**，而实例持有类引用——于是运行中的适配器一直是旧代码。
  现在两个绑定钩子都会检测陈旧实例并重建。
- **`mc_command` 工具描述自相矛盾**：原先写「调用前先用 `mc_karma` 扣除好感，
  你不需要自己再扣一次」，容易让 AI 反复纠结。已改为明确「好感扣除由本工具
  自己完成，不要用 `mc_karma` 再扣」。
- **`cmd` / `server` 参数说明原先硬编码且两侧不一致**，现已可配，两侧共用一份。
- **游戏侧上下文模板丢了 `<netherlink_context>` 包裹标签**：`mc_command` 的
  工具描述写着「身份见系统提示词中的 `<netherlink_context>`」，而它在游戏侧与
  QQ 侧两个面上都可用。游戏侧少了包裹标签，AI 在系统提示词里找不到它，就会把
  管理员当普通玩家拒掉。两份模板现在都有守卫钉住首尾标签。
- **游戏侧模板丢了「回复之前先调用 `mc_karma` 查一次」**：游戏侧会话按服务器建、
  玩家进出频繁，每轮都该查（QQ 侧刻意不查——主 agent 有会话记忆，每轮强制查
  等于每轮闲聊都白多一次工具往返）。这是两侧模板的唯一实质差异。
- **注入钩子的幂等判断只写在 docstring 里、代码里根本没有**：两条分支都无条件
  拼接，同一个请求被重复处理时（重试、多插件链）整段上下文会叠两次，
  AI 同时看到两份身份。已在两条分支之前补上判断。

### 提示词调整

- `karma_rules` 新增「不要暴露好感值这一存在」与「互动涉及好感度时，调用
  `mc_karma`」（原先这句是独立常量，现并入可配的好感规则）。
- `mc_command` 的 cost 参数说明新增「完全超标则直接拒绝执行」。
- `advancement_prompt` 改为**系统通知**口吻，并点明「这不是玩家对你说的话」——
  此前 AI 会把成就当成玩家在邀功。
- `extra_system_prompt` 的「玩家想用物品换取好感时」改为「玩家试图或暗示要给你
  东西时」，并强调**必须先核实背包并扣除物品再加好感**。此前玩家说
  「我给你金锭好不好」，AI 会直接道谢并加好感而**没有扣除物品**。
- 上下文模板去掉了「对话历史里方括号标注说话人」一段——原生群聊下玩家消息
  不是那种形式。

### 升级须知

- **需要重启一次 AstrBot**：平台适配器由框架在启动时实例化，
  仅重载插件不够（首次启用时）。
- 已保存过配置的部署**读的是落盘的那份**，不会自动采用新的默认值。
- 已删除的配置项：`template_netherlink_request`、`extra_system_prompt`、
  `admin_context_admin_only`（残留无害，插件不再读取）。
- ⚠️ **所有提示词配置项现在都是「空串回退默认值」**（此前 `extra_system_prompt`
  是「留空 = 不附加」的另一套口径）。想不注入某段内容，请**删掉模板里的正文**，
  不要清空配置项——清空只会让默认值静默回来。


## v0.0.5

本版重做了提示词注入的架构，并调整了若干默认值。

### 变更

- **注入方式简化为两个场景**：以前有四个注入面（游戏内对话、游戏内成就、
  QQ 群请求执行指令、QQ 群普通对话），配置起来要在多处来回找。现在收敛为
  **QQ 侧**与**游戏侧**两个模板，各自一份，配置项说明里逐项写清了每段是什么。
- **QQ 侧的普通对话与指令对话统一**：此前群友请求执行指令时会额外起一个
  内部对话来复核报价，现在规则直接注入主对话，**省去一次往返**，
  报价也更直接。
- **指令的执行尺度重新分配**：价目表与「免费放行 / 可以执行 / 按超标程度计价」
  移到了工具的参数说明里（闲聊时不再占用上下文）；「一律拒绝」那类留在工具描述里，
  因为它需要在决定用不用工具之前就被看到。
- **好感规则现在对所有场景生效**：此前 QQ 群里的普通聊天拿不到好感度规则，
  AI 不知道这套机制存在。
- **两处好感引导句去冗余**：游戏侧那句去掉了机器人名（内层对话删除后已无意义），
  QQ 侧那句从「需要判断对方好感时」改为「互动涉及好感度时」——前者是同义反复，
  后者给出可识别的触发场景。参数怎么传仍由工具描述负责，不在此重复。

### 默认值调整

| 配置项 | 新默认值 |
|---|---|
| 聊天推送模板 | `⌜{server}⌟ <{player}>: {text}` |
| 死亡推送模板 | `⌜{server}⌟ {message}` |
| QQ 消息转发进游戏的格式 | `§a⌜{group}⌟§f <§b{sender}§f> §d{text}§f` |
| 机器人在游戏内回复格式 | `§c⌜{bot}⌟§f : §d{text}§f` |
| 初始好感 | 20（原 10）|
| 单次指令消耗上限 | 80（原 100）|

> ⚠️ **升级提示**：已保存过配置的部署不会自动采用新默认值——请到 WebUI 确认
> 这几项。另外旧配置项 `template_netherlink_context` 已被
> `template_netherlink_context_qq` / `template_netherlink_context_game` 取代。

## v0.0.4

本版把注入给 AI 的提示词文本全部交由 WebUI 配置，并补记 v0.0.3 之后未上架的改动。

### 新增

- **系统上下文模板可配**：`<netherlink_context>`（告诉 AI 消息来自哪里、管理员是谁、
  发起者是谁、回复前先查好感）原先硬编码，现在由新配置项
  `template_netherlink_context` 渲染。占位符：`{origin}` 来源整行、
  `{roster}` 管理员名单整行、`{identity}` 当前发起者、`{is_admin}` 是/不是管理员。
  这段文本注入**四个场景**（游戏内对话、获得成就、QQ 群友请求执行指令、QQ 群普通对话），
  改一处即四场景同时生效。
- **游戏内附加说明可配**：`<netherlink_request>`（游戏内对话时跟玩家那句话一起
  发出的工具使用说明）由新配置项 `template_netherlink_request` 渲染。
- 两个模板都支持：空串回退默认值；占位符写错或漏写必需占位符时回退默认值
  并记一条 warning，不会把带字面量 `{foo}` 的文本发给 AI。
- **身份注入与好感度放宽到所有群**：`target_groups` 现在**只管消息互通**（消息转发与
  事件推群）。身份注入不再限定绑定群——任何 `aiocqhttp` 群里的群友都能被 AI 认出
  （是谁、是不是管理员），好感度本来就一直如此。私聊与其他平台仍不注入（避免污染
  其他插件与其他平台的提示词）。

- **指令的执行尺度写进提示词**：`karma_rules` 新增四类明确尺度——免费放行（纯查询、
  玩家自己短距传送）、可以执行（造生物取乐、给本人普通物品）、**一律拒绝**（清空成就
  进度这类可反复刷的、召唤末影龙/凋零等影响世界的、伤害其他玩家的）、超规格物品
  按超标程度计价。想调整边界，改这段提示词即可。
- **成就好感有了量级**：`advancement_prompt` 现在写明按成就难度给 2~10
  （此前一个数都没有，AI 只能自由发挥）。
- **用物品换好感的流程**：`extra_system_prompt` 新增三步规定——必须先查背包确认
  有该物品、只按玩家说出的数量取走、最后才加好感；并明令禁止不带数量的清理
  （那会清空该物品全部）。
- **说话人不再认错**：会话按服务器建，同一会话里有多名玩家的发言，现在系统上下文
  会说明历史里以方括号标注说话人，AI 不会再把别人的话当成当前发起者说的。

### 修复（v0.0.3 之后、未上架的改动）

- **多服务器时群消息前缀恒为 `[MC]`**：玩家在游戏里唤醒机器人时，插件补推玩家原话
  那一步漏传了服务器标识，导致 `{server}` 展开成兜底值。现已修正。


- **`mc_karma` 的 delta 参数说明改不动**：代码读的键名少写了 `mc_` 前缀
  （`karma_delta_param_desc` vs schema 的 `mc_karma_delta_param_desc`），
  该配置项自加入起就是死的——在 WebUI 改它毫无效果，且不会报错。
  现已修正，并新增守卫确保所有 `config.get` 的键都在配置定义里存在。
- **游戏侧的 `delta` 参数说明是硬编码的**：它没走配置项，用户改了只影响 QQ 侧。
  现已与 QQ 侧同源。
- **管理员在 QQ 群里认不出来**：群友只是跟 AI 聊天时，AI 完全不知道对方是谁
  （身份只在他请求执行指令时才注入）。现在通过 AstrBot 的 LLM 请求钩子，绑定群的
  普通对话也会带上发起者身份与管理员判定。
- **价目表占据每一轮对话**：指令的好感价目表原先写在始终注入的 `karma_rules` 里，
  闲聊时也占上下文。现移到 `mc_command` 的 cost 参数说明里（配合 AstrBot 的
  skills-like 工具调用模式，只有 AI 选中该工具后才加载）。
- 工具的参数说明（cost / delta）也做成可配置项。
- 全部提示词与配置文本改为半角标点、去掉 markdown 记号。

## v0.0.3

> 本版把自 v0.0.1 以来累积的修复与新功能一并发布（v0.0.2 为内部版本号，
> 未单独上架）。**升级后建议重新部署 Paper 端插件**——多处修复需要两端一起更新。

### 修复

- **群消息全部静默丢失**：主动推送原本把平台标识写死成 `aiocqhttp`，而
  AstrBot 的会话标识首段实际是 WebUI 里填的「机器人名称」。除了恰好这么命名的
  部署，其余部署下游戏聊天/进退服/死亡/成就/机器人回复在群里**一条都收不到**，
  且 AstrBot 只记一条 `cannot find platform` 警告、插件毫不知情。现在按真实平台
  实例解析，并显式接住 `send_message` 的返回值补日志。
- **接收在第一条消息后永久停止**：Paper 端覆写 WebSocket 回调时漏了「申请下一次
  接收」，导致握手正常、第一条下行能收到，之后**永久静默且不报任何错**。
  表现为「游戏里第一句话 AI 回了，第二句石沉大海」。
- **执行指令必然失败**：自造的控制台发送器不被 Paper 接受，任何**原生指令**
  （`give`/`tp`/`list`/`time`…）都会抛
  `Cannot make ... a vanilla command listener`。已改用官方的
  `Bukkit.createCommandSender`，它同时能捕获原生指令与插件指令的输出。
- **执行失败却报「已执行」并扣好感**：指令回执的成功标志两端同时失效（发出方写死
  为成功、接收方又没读它），于是服务器明确报错时，AI 依然被告知
  「指令已执行（消耗好感 N）」，好感被真扣。现在四种结局分开处理
  （成功 / 成功无输出 / **明确失败（退费）** / 无回执（退费）），缺字段一律按失败处理。
- **管理员被当成普通玩家**：身份信息在系统提示词与用户消息里各发了一份同名标签，
  而工具描述让 AI「以对话中的为准」，于是采信了那份**没有管理员信息**的。
  配置、判定逻辑全都正确，只有 AI 看到的被遮蔽了。已改用不同标签名。
- **游戏内唤醒消息不推群**：玩家发唤醒词对话时，群里只看得到 AI 的回答、看不到
  玩家问了什么。现在玩家原话也会按聊天模板推群。
- **管理员配了却不起作用（主因）**：指令工具的描述把 AI 指向了
  「**对话中**提供的身份信息」，但身份信息实际只写在**系统提示词**里，对话中
  根本没有；描述还补了一句「若未提供，则按普通玩家对待」。AI 于是有据可依地
  在对话里找不到名单，把管理员当成普通玩家——游戏内和 QQ 群两条链路都是如此。
  现在改为指向系统提示词。
- **管理员配了却不起作用（两个次要原因，一并修掉）**：
  ① 用中文输入法打出的分隔符（全角逗号「，」、顿号「、」）会让整串被当成一个名字，
  现在这些分隔符都会被正确识别；
  ② 游戏 ID 的匹配原先区分大小写，而 MC 登录名不区分
  （填 `moedawn`、游戏里是 `MoeDawn` 就匹配不上），现在改为大小写不敏感。
  另外启动时会打印一条配置解析结果日志，方便直接确认名单到底被读成了什么。

### 新功能

- **QQ 群里的普通对话也能认出管理员了**：此前只有群友请 AI「执行指令」时插件才会
  告诉他对方是谁；单纯聊天时 AI 完全不知道发言者身份。现在绑定群内的对话都会带上
  发言者与管理员判定。
- **多服务器支持**：一台 MC 服务器占一个 AstrBot 端口（配置 `ws_ports`，
  如 `survival:8765,creative:8766`）。两台服务器连同一个端口会互相踢下线，
  现在会**明确拒绝并报错**，不再静默抖动。
- **按服务器区分显示名**：配置 `server_display_names`（如
  `survival:生存服,creative:创造服`），群里分别显示 `[生存服]` `[创造服]`。
  显示名始终由本插件决定，MC 端只上报身份。
- **会话按服务器**：同一服务器上多个玩家的对话在同一个会话里，AI 更容易掌握
  服务器上的整体情况。
- **指令定向**：游戏内发起的指令自动发往发起者所在的那台服务器；
  QQ 侧由 AI 依上下文选择目标，只有一台在线时可直接省略。
- **管理员更宽松的尺度**：管理员身份会被告知 AI，并可在 `karma_rules` 里
  自定义该给他多大的通融空间。
- **每次回复前都会先查一次好感**：AI 现在被明确要求在回话之前先调用好感度工具，
  再据此决定态度。这条要求写在四类对话共用的那段上下文里，因此**群里的普通聊天
  也适用**（此前那条路径看不到好感规则，AI 往往直接回话、跳过查询）。

### 移除

- **`ws_port`（单端口）已删除**——它只能填一个端口，是多服务器配置 `ws_ports` 的
  子集。请改用 `ws_ports`，**单台服务器也只需填一个**（如 `8765`）。
  ⚠️ **留空则不会监听任何端口**（启动时报错提示），不会再回退到旧的那个默认端口。
- **`mc_server_name`（单服务器名）已删除**——同理由 `server_display_names` 取代，
  没配映射的服务器统一显示为 `MC`。
- 计分板时代的 `karma_rules` 自动迁移逻辑（含特征串常量）——属历史遗留代码。
  注意：若配置里仍存着计分板时代的旧文案，插件**不再自动纠正也不告警**，
  请到 WebUI 确认 `karma_rules` 是真正的规则。

### 变更

- **指令的好感价目表已移出对话上下文**：原先每次对话（含闲聊）都会携带整张价目表，
  现已移入 `mc_command` 的 **cost 参数说明**——只在 AI 决定执行指令时才需要它。
  配套建议把 AstrBot 的「工具调用模式」设为 **skills-like**（两阶段），
  参数说明才会延迟加载；用默认的 full 模式则价目表仍会每轮下发。
- **工具的参数说明现在也可配置**：新增 `mc_command_cost_param_desc` 与
  `mc_karma_delta_param_desc` 两个配置项（价目表写在前者里）。
- 插件显示名：`NetherLink MC×QQ互通` → **`NetherLink 群服互通`**
- 移除 `metadata.yaml` 的 `logo` 字段（`logo.png` 文件仍随包提供）
- 好感度规则的成本表重写，覆盖更多指令类别

## v0.0.1

首个版本。MC 服务器与 QQ 群双向消息互通、自然语言执行 MC 指令、游戏内机器人对话。

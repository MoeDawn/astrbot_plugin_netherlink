# 更新日志

## v0.0.3

> 本版把自 v0.0.1 以来累积的修复与新功能一并发布（v0.0.2 为内部版本号，
> 未单独上架）。**升级后建议重新部署 Paper 端插件**——多处修复需要两端一起更新。

### 修复

- **群消息全部静默丢失**：主动推送原本把平台标识写死成 `aiocqhttp`，而
  AstrBot 的会话标识首段实际是 WebUI 里填的「机器人名称」。除了恰好这么命名的
  部署，其余部署下游戏聊天/进退服/死亡/成就/机器人回复在群里**一条都收不到**，
  且 AstrBot 只记一条 `cannot find platform` 警告、插件毫不知情。现在按真实平台
  实例解析，并显式接住 `send_message` 的返回值补日志。
- **接收在第一条消息后永久停止**：Paper 端覆写 WebSocket 回调时漏了「申请下一次
  接收」，导致握手正常、第一条下行能收到，之后**永久静默且不报任何错**。
  表现为「游戏里第一句话 AI 回了，第二句石沉大海」。
- **执行指令必然失败**：自造的控制台发送器不被 Paper 接受，任何**原生指令**
  （`give`/`tp`/`list`/`time`…）都会抛
  `Cannot make ... a vanilla command listener`。已改用官方的
  `Bukkit.createCommandSender`，它同时能捕获原生指令与插件指令的输出。
- **执行失败却报「已执行」并扣好感**：指令回执的成功标志两端同时失效（发出方写死
  为成功、接收方又没读它），于是服务器明确报错时，AI 依然被告知
  「指令已执行（消耗好感 N）」，好感被真扣。现在四种结局分开处理
  （成功 / 成功无输出 / **明确失败（退费）** / 无回执（退费）），缺字段一律按失败处理。
- **管理员被当成普通玩家**：身份信息在系统提示词与用户消息里各发了一份同名标签，
  而工具描述让 AI「以对话中的为准」，于是采信了那份**没有管理员信息**的。
  配置、判定逻辑全都正确，只有 AI 看到的被遮蔽了。已改用不同标签名。
- **游戏内唤醒消息不推群**：玩家发唤醒词对话时，群里只看得到 AI 的回答、看不到
  玩家问了什么。现在玩家原话也会按聊天模板推群。
- **管理员配了却不起作用（主因）**：指令工具的描述把 AI 指向了
  「**对话中**提供的身份信息」，但身份信息实际只写在**系统提示词**里，对话中
  根本没有；描述还补了一句「若未提供，则按普通玩家对待」。AI 于是有据可依地
  在对话里找不到名单，把管理员当成普通玩家——游戏内和 QQ 群两条链路都是如此。
  现在改为指向系统提示词。
- **管理员配了却不起作用（两个次要原因，一并修掉）**：
  ① 用中文输入法打出的分隔符（全角逗号「，」、顿号「、」）会让整串被当成一个名字，
  现在这些分隔符都会被正确识别；
  ② 游戏 ID 的匹配原先区分大小写，而 MC 登录名不区分
  （填 `moedawn`、游戏里是 `MoeDawn` 就匹配不上），现在改为大小写不敏感。
  另外启动时会打印一条配置解析结果日志，方便直接确认名单到底被读成了什么。

### 新功能

- **多服务器支持**：一台 MC 服务器占一个 AstrBot 端口（配置 `ws_ports`，
  如 `survival:8765,creative:8766`）。两台服务器连同一个端口会互相踢下线，
  现在会**明确拒绝并报错**，不再静默抖动。
- **按服务器区分显示名**：配置 `server_display_names`（如
  `survival:生存服,creative:创造服`），群里分别显示 `[生存服]` `[创造服]`。
  显示名始终由本插件决定，MC 端只上报身份。
- **会话按服务器**：同一服务器上多个玩家的对话在同一个会话里，AI 更容易掌握
  服务器上的整体情况。
- **指令定向**：游戏内发起的指令自动发往发起者所在的那台服务器；
  QQ 侧由 AI 依上下文选择目标，只有一台在线时可直接省略。
- **管理员更宽松的尺度**：管理员身份会被告知 AI，并可在 `karma_rules` 里
  自定义该给他多大的通融空间。

### 变更

- 插件显示名：`NetherLink MC×QQ互通` → **`NetherLink 群服互通`**
- 移除 `metadata.yaml` 的 `logo` 字段（`logo.png` 文件仍随包提供）
- 好感度规则的成本表重写，覆盖更多指令类别

## v0.0.1

首个版本。MC 服务器与 QQ 群双向消息互通、自然语言执行 MC 指令、游戏内机器人对话。
